# Dashboard-only dependencies for Streamlit Cloud
# No version pins — allows Streamlit to use pre-built cached wheels
# which avoids slow C-extension compilation (especially snowflake-connector-python)

streamlit
plotly
pandas
snowflake-connector-python
python-dotenv
