# Dashboard-only dependencies — used by Streamlit Cloud
# Much lighter than the full requirements.txt which includes
# pipeline-only packages (groq, google-generativeai, etc.)

streamlit==1.35.0
plotly==5.22.0
pandas==2.2.2
snowflake-connector-python==3.10.0
python-dotenv==1.0.1
